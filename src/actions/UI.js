import * as types from 'actionTypes';

export function maximizeChat() {
    return {
        type: types.MAXIMIZE_CHAT
    }
}

export function minimizeChat() {
    return {
        type: types.MINIMIZE_CHAT
    }
}
