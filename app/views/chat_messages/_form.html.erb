<%= form_for(ChatMessage.new, remote: true) do |f| %>

  <%= f.hidden_field :issue_id, value: @issue.id %>
  <%= f.hidden_field :user_id, value: User.current.id %>

  <%= f.text_area :message %>

  <%= f.button 'Send', id: 'message-to-send' %>
<% end %>
